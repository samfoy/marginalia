# marginalia dependencies
# ⚠ DO NOT run `pip install -r requirements.txt` — this installs ALL providers.
# Instead, install only what you need via pyproject.toml extras:
#
#   pip install -e ".[openai,embed]"    # OpenAI + local embeddings
#   pip install -e ".[anthropic,embed]" # Anthropic + local embeddings
#   pip install -e ".[bedrock]"         # AWS Bedrock
#   pip install -e ".[all]"             # everything
#
# This file is a reference listing only — for CI or advanced users.

# Core (always required)
numpy

# AWS Bedrock (required if using Bedrock or bedrock-mantle)
boto3>=1.28
botocore>=1.31

# OpenAI direct API (required if MARGINALIA_OPENAI_API_KEY is set)
openai>=1.0

# Anthropic direct API (required if MARGINALIA_ANTHROPIC_API_KEY is set)
anthropic>=0.20

# Local embeddings (required if MARGINALIA_EMBED_BACKEND=local or no API keys)
sentence-transformers>=2.0
